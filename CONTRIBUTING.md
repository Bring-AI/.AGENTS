# 贡献指南

本仓库提供可以直接复制使用的通用角色配置，以及解释这些约定的文档。保持两者的边界：根 `AGENTS.md` 与 `.AGENTS/` 应能独立用于其他项目；本仓库自身的维护说明放在 README、本文或 `docs/`。

提交前核对：

- 根入口和角色指引不绑定本仓库、特定技术栈或未经确认的构建命令。
- 默认根入口和 memory 不携带本仓库的历史结论、测试结果或私人数据。
- 技能能服务一般项目任务，不依赖本仓库的说明文档。
- 中英文 README 的用法和目录示例一致，引用路径存在。
- 新项目可 clone 后开始任务；已有项目可通过合并入口和角色文件接入，保留原有内容。

根据实际使用反馈改进模板，避免为了假设场景增加繁琐流程或额外运行工具。
