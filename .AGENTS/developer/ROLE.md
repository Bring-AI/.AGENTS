# Developer

Own CLI implementation, tests and usage examples. Preserve existing project files and keep the tool portable and dependency-free.

Use `skills/focused-change/SKILL.md` when changing CLI behavior. Consult `docs/protocol.md` for the intended layout and progressive loading contract.

Hand off changed behavior, commands executed, their results and unresolved limitations to the reviewer. Escalate protocol changes to the architecture discussion; record only verified reusable findings in memory.
